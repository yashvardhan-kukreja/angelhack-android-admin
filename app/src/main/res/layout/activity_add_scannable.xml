<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".Activities.AddScannableActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        app:layout_scrollFlags="scroll|enterAlways"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <Spinner
        android:id="@+id/Spinner"
        android:layout_width="fill_parent"
        android:layout_margin="16dp"
        android:layout_height="wrap_content" />

    <EditText
        android:id="@+id/scannable_name"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:hint="Name of Scannable"
        android:layout_width="match_parent"/>

    <EditText
        android:id="@+id/scannable_desc"
        android:layout_margin="16dp"
        android:hint="Description of Scannable"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/submit_scannable"
        android:layout_gravity="bottom"
        android:background="@drawable/scan_again_btn"
        android:padding="15dp"
        android:layout_margin="16dp"
        android:textColor="@color/colorPrimary"
        android:text="Add"/>

</LinearLayout>